# Truth Ledger Database Configuration
# Copy this file to .env and fill in your values

# PostgreSQL Connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rocket_engine_truth_ledger
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_SSL=false
DB_POOL_SIZE=10

# Optional: Full DATABASE_URL (overrides individual settings)
# DATABASE_URL=postgres://user:password@host:port/database
